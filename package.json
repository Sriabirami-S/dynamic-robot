{
  "name": "dynamic-robot",
  "private": true,
  "version": "1.0.0",
  "type": "module",

  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  },

  "dependencies": {
    "three": "^0.185.0"
  },

  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^8.1.0"
  }
}